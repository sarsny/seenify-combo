<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>犀帆 SEENIFY AIO 套餐选择</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  </head>
  <body class="bg-[#0f0f11]">
    <div id="root"></div>
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script type="text/babel">
    const { useState, useEffect, useMemo } = React;

// --- Icons (Inline SVGs for stability) ---
const Icon = ({ path, size = 20, className = "" }) => (
    <svg 
        xmlns="http://www.w3.org/2000/svg" 
        width={size} 
        height={size} 
        viewBox="0 0 24 24" 
        fill="none" 
        stroke="currentColor" 
        strokeWidth="2" 
        strokeLinecap="round" 
        strokeLinejoin="round" 
        className={className}
    >
        {path}
    </svg>
);

const Icons = {
    Zap: (props) => <Icon path={<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>} {...props} />,
    Check: (props) => <Icon path={<polyline points="20 6 9 17 4 12"></polyline>} {...props} />,
    Info: (props) => <Icon path={<><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line></>} {...props} />,
    Shield: (props) => <Icon path={<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>} {...props} />,
    TrendingUp: (props) => <Icon path={<polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>} {...props} />,
    Search: (props) => <Icon path={<><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></>} {...props} />,
    Database: (props) => <Icon path={<><ellipse cx="12" cy="5" rx="9" ry="3"></ellipse><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"></path><path d="M3 5v14c0 1.66 4 3 9 3s 9-1.34 9-3V5"></path></>} {...props} />,
    Crown: (props) => <Icon path={<path d="M2 4l3 12h14l3-12-6 7-4-7-4 7-6-7zm3 16h14"></path>} {...props} />,
    AlertCircle: (props) => <Icon path={<><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></>} {...props} />,
    ArrowRight: (props) => <Icon path={<><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></>} {...props} />
};

// --- Data Definitions ---

const DIFFICULTIES = [
    { id: 'blue', name: '蓝海/冷门', k: 0.8, desc: '竞品 < 5，AI语料空白' },
    { id: 'standard', name: '温带/标准', k: 1.0, desc: '竞品 5-20，有少量基础' },
    { id: 'red', name: '红海/热带', k: 1.5, desc: '竞品 > 20，巨头林立 (如猫粮/美妆)' }
];

const PACKAGES = [
    {
        id: 'L1',
        name: '体验版',
        sub: 'Experience Pack',
        basePrice: 2000,
        months: 1,
        platforms: 1,
        intents: 1,
        features: ['品牌推荐意图', '基础收录可见', '无情绪修正', '无竞品比对'],
        tag: '入门验证',
        color: 'gray'
    },
    {
        id: 'L2',
        name: '基础版',
        sub: 'Basic Pack',
        basePrice: 28800,
        months: 3,
        platforms: 3,
        intents: 2,
        features: ['推荐 + 调研意图', '头部流量铁三角', '情绪修正 (去负面)', '事实纠偏'],
        tag: '高性价比',
        color: 'blue'
    },
    {
        id: 'L3',
        name: '标准版',
        sub: 'Standard Pack',
        basePrice: 59800,
        months: 3,
        platforms: 6,
        intents: 3,
        features: ['推荐 + 调研 + 竞品比对', '全网主流覆盖', '偏好度优化 (赢竞品)', '季度动态调优 (5%)'],
        tag: '🔥 主推爆款',
        color: 'purple'
    },
    {
        id: 'L4',
        name: '旗舰版',
        sub: 'Flagship Pack',
        basePrice: 168000,
        months: 6,
        platforms: 10,
        intents: 5,
        features: ['全场景统治 (含转化)', 'DeepSeek 深度逻辑优化', '对赌排名前三', '季度动态调优 (15%)'],
        tag: '👑 行业统治',
        color: 'yellow'
    }
];

const ADDONS = [
    {
        id: 'platform',
        name: '平台扩展包',
        desc: '增加 1 个指定平台 (如小红书AI)',
        basePrice: 10000,
        type: 'counter',
        allowL1: false
    },
    {
        id: 'intent',
        name: '意图扩展包',
        desc: '增加 1 个全新对话场景 (如售后)',
        basePrice: 15000,
        type: 'counter',
        allowL1: false
    },
    {
        id: 'deepseek',
        name: 'DeepSeek 深度优化',
        desc: '推理链逻辑重构 (L4已包含)',
        basePrice: 20000,
        type: 'toggle',
        allowL1: false,
        includedIn: ['L4']
    },
    {
        id: 'sentiment',
        name: '情绪清洗专项包',
        desc: '针对特定恶评进行机理对冲',
        basePrice: 15000,
        type: 'toggle',
        allowL1: false
    }
];

const formatPrice = (price) => {
    return new Intl.NumberFormat('zh-CN', { style: 'currency', currency: 'CNY', maximumFractionDigits: 0 }).format(price);
};

function App() {
    const [difficulty, setDifficulty] = useState(DIFFICULTIES[1]);
    const [selectedPkgId, setSelectedPkgId] = useState('L3');
    const [addonConfig, setAddonConfig] = useState({
        platform: 0,
        intent: 0,
        deepseek: false,
        sentiment: false
    });

    const selectedPkg = PACKAGES.find(p => p.id === selectedPkgId);

    // Calculate Totals
    const totalPrice = useMemo(() => {
        let total = selectedPkg.basePrice * difficulty.k;

        // Add-ons
        total += (addonConfig.platform * 10000 * difficulty.k);
        total += (addonConfig.intent * 15000 * difficulty.k);
        
        if (addonConfig.deepseek && !selectedPkg.features.join('').includes('DeepSeek')) {
            total += (20000 * difficulty.k);
        }
        if (addonConfig.sentiment) {
            total += (15000 * difficulty.k);
        }

        return total;
    }, [difficulty, selectedPkg, addonConfig]);

    const monthlyPrice = totalPrice / selectedPkg.months;

    // Handlers
    const handleAddonToggle = (id) => {
        setAddonConfig(prev => ({ ...prev, [id]: !prev[id] }));
    };

    const handleCounter = (id, delta) => {
        setAddonConfig(prev => ({ 
            ...prev, 
            [id]: Math.max(0, prev[id] + delta) 
        }));
    };

    // Reset specific addons when switching packages if included
    useEffect(() => {
        if (selectedPkgId === 'L4') {
            setAddonConfig(prev => ({ ...prev, deepseek: false })); // It's included
        } else if (selectedPkgId === 'L1') {
            setAddonConfig({ platform: 0, intent: 0, deepseek: false, sentiment: false });
        }
    }, [selectedPkgId]);

    // Color Logic for Dynamic Backgrounds
    const getGlowColor = (pkgId) => {
        switch(pkgId) {
            case 'L3': return 'bg-purple-600';
            case 'L4': return 'bg-yellow-600';
            case 'L2': return 'bg-blue-600';
            default: return 'bg-gray-600';
        }
    };

    return (
        <div className="min-h-screen bg-[#0f0f11] text-white font-sans flex flex-col lg:flex-row overflow-hidden selection:bg-purple-500/30">
            <style>{`
                @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
                body { font-family: 'Inter', sans-serif; }
                ::-webkit-scrollbar { width: 6px; }
                ::-webkit-scrollbar-track { background: #18181b; }
                ::-webkit-scrollbar-thumb { background: #3f3f46; border-radius: 3px; }
                ::-webkit-scrollbar-thumb:hover { background: #52525b; }
                .glass-panel {
                    background: rgba(24, 24, 27, 0.6);
                    backdrop-filter: blur(12px);
                    -webkit-backdrop-filter: blur(12px);
                    border: 1px solid rgba(255, 255, 255, 0.08);
                }
                .animate-fade-in { animation: fadeIn 0.5s ease-out forwards; }
                @keyframes fadeIn {
                    from { opacity: 0; transform: translateY(10px); }
                    to { opacity: 1; transform: translateY(0); }
                }
            `}</style>
            
            {/* LEFT PANEL: Visualizer (Sticky on Desktop) */}
            <div className="lg:w-1/2 relative bg-[#121215] flex flex-col justify-between p-6 lg:p-12 lg:h-screen lg:sticky lg:top-0 border-r border-white/5 z-10">
                
                {/* Header */}
                <div className="mb-8">
                    <div className="flex items-center gap-2 mb-2">
                        <Icons.Zap className="text-purple-500 fill-purple-500" size={24} />
                        <h1 className="text-2xl font-bold tracking-tight">犀帆 SEENIFY <span className="text-purple-500">AIO</span></h1>
                    </div>
                    <p className="text-zinc-400 text-sm">AI 可见性优化配置终端 v6.0</p>
                </div>

                {/* Dynamic Visualization Card */}
                <div className="flex-1 flex items-center justify-center py-8">
                    <div className="w-full max-w-md">
                        {/* The "Card" */}
                        <div className="glass-panel rounded-2xl p-8 relative overflow-hidden transition-all duration-500">
                            {/* Background Glow */}
                            <div className={`absolute -top-20 -right-20 w-64 h-64 ${getGlowColor(selectedPkgId)} rounded-full mix-blend-screen filter blur-[80px] opacity-20 transition-colors duration-500`}></div>
                            <div className={`absolute -bottom-20 -left-20 w-64 h-64 bg-purple-600 rounded-full mix-blend-screen filter blur-[80px] opacity-20`}></div>

                            {/* Card Content */}
                            <div className="relative z-10">
                                <div className="flex justify-between items-start mb-6">
                                    <div>
                                        <div className="text-zinc-400 text-xs font-medium tracking-wider uppercase mb-1">{selectedPkg.sub}</div>
                                        <h2 className="text-3xl font-bold text-white mb-2">{selectedPkg.name}</h2>
                                        <div className={`inline-flex items-center px-2 py-0.5 rounded text-xs font-medium border transition-colors duration-300
                                            ${selectedPkgId === 'L3' ? 'bg-purple-500/20 text-purple-300 border-purple-500/30' : 
                                              selectedPkgId === 'L4' ? 'bg-yellow-500/20 text-yellow-300 border-yellow-500/30' : 
                                              'bg-zinc-700/50 text-zinc-300 border-zinc-600'}`}>
                                            {selectedPkg.tag}
                                        </div>
                                    </div>
                                    <div className="text-right">
                                        <div className="text-4xl font-bold text-white tracking-tight">{formatPrice(totalPrice)}</div>
                                        <div className="text-sm text-zinc-400 mt-1">
                                            月均投入 <span className="text-white font-medium">{formatPrice(monthlyPrice)}</span>
                                        </div>
                                    </div>
                                </div>

                                <div className="space-y-4 mb-8">
                                    <div className="flex justify-between items-center py-3 border-b border-white/10">
                                        <span className="text-zinc-400 flex items-center gap-2"><Icons.Database size={16}/> 覆盖平台</span>
                                        <span className="text-white font-semibold">
                                            {selectedPkgId === 'L4' ? '全平台' : `${selectedPkg.platforms} 个`}
                                            {addonConfig.platform > 0 && <span className="text-purple-400 ml-1">+{addonConfig.platform}</span>}
                                        </span>
                                    </div>
                                    <div className="flex justify-between items-center py-3 border-b border-white/10">
                                        <span className="text-zinc-400 flex items-center gap-2"><Icons.Search size={16}/> 覆盖意图</span>
                                        <span className="text-white font-semibold">
                                            {selectedPkg.intents} 个
                                            {addonConfig.intent > 0 && <span className="text-purple-400 ml-1">+{addonConfig.intent}</span>}
                                        </span>
                                    </div>
                                    <div className="flex justify-between items-center py-3 border-b border-white/10">
                                        <span className="text-zinc-400 flex items-center gap-2"><Icons.Shield size={16}/> 服务周期</span>
                                        <span className="text-white font-semibold">{selectedPkg.months * 30} 天</span>
                                    </div>
                                    <div className="flex justify-between items-center py-3 border-b border-white/10">
                                        <span className="text-zinc-400 flex items-center gap-2"><Icons.TrendingUp size={16}/> 难度系数 (K)</span>
                                        <span className={`font-mono font-bold ${difficulty.k > 1 ? 'text-red-400' : 'text-emerald-400'}`}>
                                            x {difficulty.k.toFixed(1)}
                                        </span>
                                    </div>
                                </div>

                                {/* Features List */}
                                <div className="space-y-2">
                                    {selectedPkg.features.map((feat, i) => (
                                        <div key={i} className="flex items-center gap-2 text-sm text-zinc-300">
                                            <Icons.Check size={14} className="text-purple-500" />
                                            {feat}
                                        </div>
                                    ))}
                                    {/* Dynamic Addon Features */}
                                    {addonConfig.deepseek && selectedPkgId !== 'L4' && (
                                        <div className="flex items-center gap-2 text-sm text-purple-300 animate-fade-in">
                                            <Icons.Check size={14} className="text-purple-500" />
                                            DeepSeek 深度逻辑优化
                                        </div>
                                    )}
                                    {addonConfig.sentiment && (
                                        <div className="flex items-center gap-2 text-sm text-purple-300 animate-fade-in">
                                            <Icons.Check size={14} className="text-purple-500" />
                                            情绪清洗专项包
                                        </div>
                                    )}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                {/* Left Footer (Desktop Only) */}
                <div className="hidden lg:block text-xs text-zinc-500">
                    * 最终报价以系统生成的合同为准。红海行业（系数&gt;1.2）交付标准承诺为行业Top3或对标竞品。
                </div>
            </div>

            {/* RIGHT PANEL: Controls (Scrollable) */}
            <div className="lg:w-1/2 h-full lg:h-screen overflow-y-auto bg-black p-6 lg:p-12 pb-32">
                
                {/* Step 1: Difficulty */}
                <div className="mb-12 animate-fade-in" style={{animationDelay: '0.1s'}}>
                    <h3 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-4 flex items-center gap-2">
                        01. 行业环境定级
                        <div className="group relative">
                            <Icons.Info size={14} className="cursor-help text-zinc-600 hover:text-white transition-colors"/>
                            <div className="absolute left-0 bottom-full mb-2 w-64 bg-zinc-800 text-xs p-3 rounded shadow-xl hidden group-hover:block z-50 pointer-events-none">
                                基于《诊断报告》确定。竞品越多，系数越高，价格越高。
                            </div>
                        </div>
                    </h3>
                    <div className="grid grid-cols-3 gap-3">
                        {DIFFICULTIES.map(d => (
                            <button
                                key={d.id}
                                onClick={() => setDifficulty(d)}
                                className={`p-4 rounded-xl border text-left transition-all duration-200 ${
                                    difficulty.id === d.id 
                                    ? 'bg-zinc-800 border-purple-500 text-white shadow-lg shadow-purple-900/10' 
                                    : 'bg-[#121215] border-zinc-800 text-zinc-500 hover:border-zinc-600 hover:text-zinc-300'
                                }`}
                            >
                                <div className="font-semibold mb-1">{d.name}</div>
                                <div className="text-xs opacity-70 mb-2">系数 {d.k}</div>
                                <div className="text-[10px] leading-tight opacity-50">{d.desc}</div>
                            </button>
                        ))}
                    </div>
                </div>

                {/* Step 2: Packages */}
                <div className="mb-12 animate-fade-in" style={{animationDelay: '0.2s'}}>
                    <h3 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-4">02. 选择核心套餐</h3>
                    <div className="space-y-4">
                        {PACKAGES.map(pkg => {
                            const isActive = selectedPkgId === pkg.id;
                            const effectivePrice = pkg.basePrice * difficulty.k;
                            
                            return (
                                <div 
                                    key={pkg.id}
                                    onClick={() => setSelectedPkgId(pkg.id)}
                                    className={`relative cursor-pointer p-5 rounded-xl border transition-all duration-300 overflow-hidden group ${
                                        isActive 
                                        ? 'bg-[#121215] border-purple-500 shadow-lg shadow-purple-900/10' 
                                        : 'bg-[#121215] border-zinc-800 hover:border-zinc-600'
                                    }`}
                                >
                                    {pkg.id === 'L3' && (
                                        <div className="absolute top-0 right-0 bg-purple-600 text-white text-[10px] px-2 py-1 rounded-bl font-bold z-10">
                                            RECOMMENDED
                                        </div>
                                    )}
                                    
                                    <div className="flex justify-between items-center mb-2">
                                        <div className="flex items-center gap-3">
                                            <div className={`w-5 h-5 rounded-full border-2 flex items-center justify-center transition-colors duration-300
                                                ${isActive ? 'border-purple-500' : 'border-zinc-600 group-hover:border-zinc-500'}`}>
                                                {isActive && <div className="w-2.5 h-2.5 rounded-full bg-purple-500" />}
                                            </div>
                                            <span className={`font-bold transition-colors duration-300 ${isActive ? 'text-white' : 'text-zinc-400 group-hover:text-zinc-200'}`}>
                                                {pkg.name}
                                            </span>
                                            {pkg.tag && isActive && (
                                                <span className="text-[10px] bg-white/10 px-1.5 py-0.5 rounded text-zinc-300">
                                                    {pkg.tag}
                                                </span>
                                            )}
                                        </div>
                                        <div className="font-mono text-sm text-zinc-300">
                                            {formatPrice(effectivePrice)}
                                        </div>
                                    </div>
                                    
                                    {/* Expanded details when active */}
                                    <div className={`pl-8 text-xs text-zinc-500 transition-all duration-300 ${isActive ? 'max-h-20 opacity-100 mt-2' : 'max-h-0 opacity-0 overflow-hidden'}`}>
                                        <p className="mb-2">包含 {pkg.platforms} 个平台 · {pkg.intents} 个意图 · {pkg.months} 个月服务</p>
                                        <p>{pkg.features.join(' · ')}</p>
                                    </div>
                                </div>
                            );
                        })}
                    </div>
                </div>

                {/* Step 3: Add-ons */}
                {selectedPkgId !== 'L1' && (
                    <div className="mb-12 animate-fade-in" style={{animationDelay: '0.3s'}}>
                        <h3 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-4">03. 乐高式个性化加装</h3>
                        <div className="grid grid-cols-1 gap-4">
                            {ADDONS.map(addon => {
                                // Skip if not allowed for L1 or if included in pkg
                                if (addon.includedIn?.includes(selectedPkgId)) return null;

                                const currentVal = addonConfig[addon.id];
                                const effectivePrice = addon.basePrice * difficulty.k;

                                return (
                                    <div key={addon.id} className="p-4 rounded-xl border border-zinc-800 bg-[#121215] flex justify-between items-center hover:border-zinc-700 transition-colors">
                                        <div>
                                            <div className="font-medium text-sm text-zinc-200">{addon.name}</div>
                                            <div className="text-xs text-zinc-500">{addon.desc}</div>
                                            <div className="text-xs text-purple-400 mt-1">
                                                +{formatPrice(effectivePrice)}
                                            </div>
                                        </div>

                                        {addon.type === 'counter' ? (
                                            <div className="flex items-center gap-3 bg-zinc-900 rounded-lg p-1 border border-zinc-800">
                                                <button 
                                                    onClick={(e) => { e.stopPropagation(); handleCounter(addon.id, -1); }}
                                                    className="w-8 h-8 flex items-center justify-center text-zinc-400 hover:bg-zinc-800 rounded hover:text-white disabled:opacity-30 disabled:cursor-not-allowed"
                                                    disabled={currentVal === 0}
                                                >
                                                    -
                                                </button>
                                                <span className="w-4 text-center text-sm font-mono text-zinc-200">{currentVal}</span>
                                                <button 
                                                    onClick={(e) => { e.stopPropagation(); handleCounter(addon.id, 1); }}
                                                    className="w-8 h-8 flex items-center justify-center text-zinc-400 hover:bg-zinc-800 rounded hover:text-white"
                                                >
                                                    +
                                                </button>
                                            </div>
                                        ) : (
                                            <button 
                                                onClick={() => handleAddonToggle(addon.id)}
                                                className={`w-12 h-6 rounded-full p-1 transition-colors duration-200 ease-in-out focus:outline-none ${currentVal ? 'bg-purple-600' : 'bg-zinc-700'}`}
                                            >
                                                <div className={`w-4 h-4 bg-white rounded-full transition-transform duration-200 shadow-sm ${currentVal ? 'translate-x-6' : 'translate-x-0'}`} />
                                            </button>
                                        )}
                                    </div>
                                );
                            })}
                            
                            {/* L4 Included Message */}
                            {selectedPkgId === 'L4' && (
                                <div className="p-4 rounded-xl border border-yellow-900/30 bg-yellow-900/10 flex items-start gap-3">
                                    <Icons.Crown size={18} className="text-yellow-500 mt-1" />
                                    <div>
                                        <div className="text-sm font-bold text-yellow-500">已解锁 L4 尊享权益</div>
                                        <div className="text-xs text-yellow-200/70 mt-1">
                                            已包含 DeepSeek 深度优化、季度动态调优、电商转化引导。
                                            无需额外购买。
                                        </div>
                                    </div>
                                </div>
                            )}
                        </div>
                    </div>
                )}

                {/* Order Action (Mobile Sticky) */}
                <div className="fixed bottom-0 left-0 right-0 lg:static lg:bg-transparent bg-black/90 backdrop-blur-xl border-t lg:border-none border-zinc-800 p-6 lg:p-0 z-50">
                        <div className="lg:hidden flex justify-between items-center mb-4">
                        <div>
                            <div className="text-xs text-zinc-400">预计总投入</div>
                            <div className="text-2xl font-bold text-white">{formatPrice(totalPrice)}</div>
                        </div>
                        <div className="text-right">
                                <div className="text-xs text-zinc-400">月均</div>
                                <div className="text-sm font-medium text-white">{formatPrice(monthlyPrice)}</div>
                        </div>
                        </div>
                        
                        <button className="w-full bg-white text-black hover:bg-zinc-200 font-bold py-4 rounded-full transition-all flex items-center justify-center gap-2 group active:scale-95">
                        <span>生成方案与合同</span>
                        <Icons.ArrowRight size={18} className="group-hover:translate-x-1 transition-transform"/>
                    </button>
                    <p className="text-center text-[10px] text-zinc-600 mt-4 lg:text-left">
                        点击即表示您同意《犀帆AIO服务条款》。K值系数最终解释权归犀帆所有。
                    </p>
                </div>
                
            </div>
        </div>
    );
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
    </script>
  </body>
</html>
